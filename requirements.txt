# Herald - Text-to-Speech Utility
# Python 3.10+

# TTS Engines
pyttsx3>=2.90          # Offline TTS (Windows SAPI)
edge-tts>=6.1.0        # Online TTS (Microsoft Azure neural voices)

# Audio playback (for edge-tts)
pygame>=2.5.0

# Global hotkey listener (requires admin on Windows)
keyboard>=0.13.5

# Clipboard access
pyperclip>=1.8.2

# Logging (rotating file logs for debugging)
loguru>=0.7.2

# System tray
pystray>=0.19.4
Pillow>=10.0.0

# OCR (Windows built-in)
winocr>=0.0.5

# Version comparison for update checker
packaging>=23.0
